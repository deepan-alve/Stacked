# Keep the data directory in git but ignore the database files
# Database is now tracked in git - remove these comments to ignore again
# *.db
*.db-journal
*.db-wal
*.db-shm

# Keep this file
!.gitignore
